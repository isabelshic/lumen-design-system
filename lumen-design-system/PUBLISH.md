# Publish checklist

- [ ] Copy `lumen-design-system/` out of the portfolio repo (or work in a dedicated clone).
- [ ] `git init` → commit → add `origin` → `git push`.
- [ ] Optional: set the GitHub repo **About** description to: *Lumen — design system for isabelshic.com (`/design-system`).*
- [ ] Optional: add the live URL `https://isabelshic.com/design-system` to the repo **Website** field.

The portfolio app remains the **source of truth** for implementation; this repo is documentation and a file map unless you later extract code into packages.
